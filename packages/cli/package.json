{
  "name": "owndesign",
  "version": "0.1.5",
  "private": false,
  "type": "module",
  "bin": {
    "owndesign": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm --filter @owndesign/server build && pnpm --filter @owndesign/web build && node scripts/build.mjs",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
