{
  "name": "owndesign",
  "version": "0.2.2",
  "private": false,
  "repository": {
    "type": "git",
    "url": "https://github.com/cyhuajuan/owndesign",
    "directory": "packages/cli"
  },
  "bin": {
    "owndesign": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "type": "module",
  "scripts": {
    "build": "pnpm --filter @owndesign/server build && pnpm --filter @owndesign/web build && node scripts/build.mjs",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest"
  },
  "dependencies": {
    "playwright-core": "^1.61.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  },
  "engines": {
    "node": ">=22"
  }
}
