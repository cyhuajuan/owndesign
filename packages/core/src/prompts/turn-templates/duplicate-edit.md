当前任务要求基于已有页面创建副本并编辑。
必须先使用 `copyFile` 将源页面复制到目标页面，然后修改目标 HTML 页面。
可以检查其他文件，但不要修改其他 HTML 页面，除非共享组件、共享导航或导航链接维护规则明确要求协调更新。

源页面：{{sourcePath}}
目标页面：{{targetPath}}
当前预览页面：{{currentPreviewPath}}

用户具体要求：
{{originalUserPrompt}}
